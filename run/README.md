# Unreal Engine Launch Scripts

These scripts launch packaged Client and Server builds from the `Build` folder.

Package the required targets before using them.

## Scripts

* `run_server.bat` — launches the Dedicated Server.
* `run_listen_server.bat` — launches the Listen Server.
* `run_2_clients.bat` — launches two Client instances.
