# Unreal Engine Target Scripts

These scripts generate Client and Server target files.

* `create_client_target.bat` — creates `MyGameClient.Target.cs`.
* `create_server_target.bat` — creates `MyGameServer.Target.cs`.
